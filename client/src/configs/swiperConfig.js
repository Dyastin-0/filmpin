import { Navigation } from 'swiper/modules';

export const swiperConfig = {
  speed: 1000,
  loop: true,
  spaceBetween: 14,
  centeredSlides: true,
  slidesPerView: 'auto',
  navigation: true,
  lazy: 'true',
  modules: [Navigation],
  className: 'swiper-slide',
};